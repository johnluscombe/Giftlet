%h1 Recipients
%br
- if @users.count > 0
  %table.table.table-hover
    %col{ width: '98%' }
    %col{ width: '1%' }
    %col{ width: '1%' }
    = render @users
- else
  %p Nobody else has registered yet!